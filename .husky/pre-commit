#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Skip linting for specific conditions
if [ "$SKIP_LINT" = "true" ]; then
  echo "⏭️  Skipping lint-staged (SKIP_LINT=true)"
  exit 0
fi

npx lint-staged --quiet || {
  echo "❌ Linting failed. Run 'pnpm lint:fix' to fix issues automatically"
  echo "💡 Or use 'SKIP_LINT=true git commit' to skip linting"
  exit 1
}